---
global:
  measurements:
    - name: podLatency
  indexerConfig:
    enabled: true
    esServers:
      - http://0.0.0.0:9200
    insecureSkipVerify: true
    defaultIndex: kraken
    type: elastic